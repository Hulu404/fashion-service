# Public Supabase credentials — used by the browser client (auth + profiles)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side Supabase credentials — used by SSR data fetching (trends, etc.)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic (matching engine, added in a later step)
ANTHROPIC_API_KEY=
